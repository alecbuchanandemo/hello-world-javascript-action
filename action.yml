name: 'Resource Usage'
description: 'Monitors CPU, Memory, and Disk usage of the Actions Runner'
# inputs:
#   who-to-greet:  # id of input
#     description: 'Who to greet'
#     required: true
#     default: 'World'
# outputs:
#   time: # id of output
#     description: 'The time we greeted you'
runs:
  using: 'node20'
  main: 'dist/index.js'